<h1>Restaurants#show</h1>
<p><%= @restaurant.name %></p>

<ul>
  <% @reviews.each do |review| %>
    <li><%= review.content %></li>
    <li><%= review.rating %></li>
  <% end %>
</ul>

<h3><%= link_to "Add a new review", new_restaurant_review_path(@restaurant) %></h3>
